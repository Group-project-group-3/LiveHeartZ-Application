import * as React from 'react';
import { DefaultComponentProps, OverrideProps } from '@mui/types';
import { FilledInputTypeMap } from './FilledInput.types';
declare const FilledInput: FilledInputComponent;
interface FilledInputComponent {
    <C extends React.ElementType>(props: {
        /**
         * The component used for the input node.
         * Either a string to use a HTML element or a component.
         * @default 'input'
         */
        inputComponent?: C;
    } & OverrideProps<FilledInputTypeMap, C>): JSX.Element | null;
    (props: DefaultComponentProps<FilledInputTypeMap>): JSX.Element | null;
    propTypes?: any;
    muiName?: string;
}
export default FilledInput;
